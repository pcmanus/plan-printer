{
  "name": "p.lan-printer",
  "version": "1.0.0",
  "description": "Compute and pretty print query plans",
  "main": "lib/index.js",
  "bin": {
    "plan-query": "./lib/index.js"
  },
  "scripts": {
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "install": "sudo npm i -g",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sylvain@apollographql.com",
  "license": "ISC",
  "dependencies": {
    "@apollo/federation": "^0.23.0",
    "@apollo/query-planner": "^0.1.1",
    "graphql": "^15.5.0",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/yargs": "^15.0.13",
    "tslint": "^6.1.3",
    "typescript": "^4.2.3"
  }
}
